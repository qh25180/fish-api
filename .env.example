# 文本文件存放目录
TEXT_FILES_DIR=./novels

# 允许的文件扩展名（逗号分隔）
TEXT_FILE_EXTENSIONS=.txt,.md

# 默认编码（auto 表示自动检测）
DEFAULT_ENCODING=auto

# 下载文件大小上限（MB）
MAX_FILE_SIZE_MB=50

# 下载超时时间（秒）
DOWNLOAD_TIMEOUT_SECONDS=30
